class Cart {
    constructor() {
        this.products = []; 
    }

    addProduct(productId, price, quantity) {
      
        this.products.push({
            productId: _id,
            name: ProductName,
            description: ProductDescription,
            price: ProductPrice,
            discount: ProductDiscount,
            category: Category,
            quantity: quantity
        });
    }

}

module.exports = Cart;