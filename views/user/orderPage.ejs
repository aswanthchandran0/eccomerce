<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout - Complete Your Order</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/assets/css/style.css">
    
    <style>
        /* Checkout Page Styles */
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3a0ca3;
            --accent-color: #7209b7;
            --success-color: #4cc9f0;
            --warning-color: #f72585;
            --light-bg: #f8f9fa;
            --dark-bg: #343a40;
        }

        .checkout-page {
            background-color: var(--light-bg);
            min-height: calc(100vh - 200px);
            padding: 40px 0;
        }

        .page-header {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: white;
            padding: 60px 0;
            text-align: center;
            margin-bottom: 40px;
            border-radius: 0 0 30px 30px;
        }

        .page-header h1 {
            font-size: 2.8rem;
            font-weight: 800;
            margin-bottom: 15px;
        }

        .page-header p {
            font-size: 1.2rem;
            opacity: 0.9;
            max-width: 600px;
            margin: 0 auto;
        }

        .checkout-container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            padding: 40px;
            margin-bottom: 40px;
        }

        .section-title {
            color: var(--dark-bg);
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 2px solid var(--light-bg);
            display: flex;
            align-items: center;
        }

        .section-title i {
            color: var(--primary-color);
            margin-right: 12px;
            font-size: 1.3rem;
        }

        /* Address Cards */
        .address-section {
            margin-bottom: 40px;
        }

        .address-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .address-card {
            border: 2px solid #eaeaea;
            border-radius: 15px;
            padding: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
            background: white;
            position: relative;
            overflow: hidden;
        }

        .address-card:hover {
            border-color: var(--primary-color);
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(67, 97, 238, 0.15);
        }

        .address-card.selected {
            border-color: var(--primary-color);
            background: linear-gradient(135deg, rgba(67, 97, 238, 0.05) 0%, rgba(58, 12, 163, 0.05) 100%);
        }

        .address-type {
            position: absolute;
            top: 0;
            right: 0;
            background: var(--primary-color);
            color: white;
            padding: 8px 15px;
            border-radius: 0 15px 0 15px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .address-details {
            margin-top: 10px;
        }

        .address-details h6 {
            color: var(--dark-bg);
            font-weight: 700;
            margin-bottom: 10px;
            font-size: 1.1rem;
        }

        .address-details p {
            color: #6c757d;
            margin-bottom: 8px;
            font-size: 0.95rem;
            line-height: 1.5;
        }

        .address-details i {
            color: var(--primary-color);
            width: 20px;
            margin-right: 8px;
        }

        .address-radio {
            position: absolute;
            bottom: 20px;
            right: 20px;
        }

        /* No Address State */
        .no-address {
            text-align: center;
            padding: 60px 20px;
            background: linear-gradient(135deg, rgba(67, 97, 238, 0.05) 0%, rgba(58, 12, 163, 0.05) 100%);
            border-radius: 15px;
            margin-bottom: 30px;
        }

        .no-address i {
            font-size: 4rem;
            color: var(--primary-color);
            margin-bottom: 20px;
            opacity: 0.7;
        }

        .no-address h4 {
            color: var(--dark-bg);
            font-weight: 700;
            margin-bottom: 10px;
        }

        .no-address p {
            color: #6c757d;
            margin-bottom: 25px;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }

        .btn-add-address {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 10px;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s ease;
        }

        .btn-add-address:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(67, 97, 238, 0.3);
            color: white;
        }

        /* Order Summary */
        .order-summary-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.05);
            padding: 30px;
            position: sticky;
            top: 100px;
            border: 1px solid #eaeaea;
        }

        .summary-header {
            text-align: center;
            margin-bottom: 25px;
        }

        .summary-header h4 {
            color: var(--dark-bg);
            font-weight: 700;
            margin-bottom: 10px;
        }

        .summary-header p {
            color: #6c757d;
            font-size: 0.9rem;
        }

        .products-list {
            max-height: 200px;
            overflow-y: auto;
            margin-bottom: 20px;
            padding-right: 10px;
        }

        .product-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid #f1f1f1;
        }

        .product-item:last-child {
            border-bottom: none;
        }

        .product-name {
            color: #495057;
            font-weight: 500;
            max-width: 70%;
        }

        .product-price {
            color: var(--primary-color);
            font-weight: 600;
        }

        .price-breakdown {
            background: var(--light-bg);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 25px;
        }

        .price-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        .price-row:last-child {
            margin-bottom: 0;
        }

        .price-label {
            color: #6c757d;
        }

        .price-value {
            color: #495057;
            font-weight: 500;
        }

        .price-total {
            font-weight: 700;
            font-size: 1.1rem;
            color: var(--primary-color);
        }

        /* Coupon Section */
        .coupon-section {
            margin-bottom: 25px;
        }

        .coupon-form {
            display: flex;
            gap: 10px;
        }

        .coupon-input {
            flex: 1;
            padding: 12px 15px;
            border: 2px solid #eaeaea;
            border-radius: 10px;
            font-size: 0.95rem;
            transition: all 0.3s ease;
        }

        .coupon-input:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.1);
            outline: none;
        }

        .btn-apply {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 10px;
            font-weight: 600;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .btn-apply:hover:not(:disabled) {
            background: var(--secondary-color);
            transform: translateY(-2px);
        }

        .btn-apply:disabled {
            background: #ccc;
            cursor: not-allowed;
            transform: none;
        }

        .coupon-applied {
            background: rgba(76, 201, 240, 0.1);
            border: 1px solid rgba(76, 201, 240, 0.3);
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 15px;
            animation: slideDown 0.3s ease;
        }

        .coupon-success {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .coupon-code {
            color: #155724;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .coupon-discount {
            color: #155724;
            font-weight: 500;
        }

        .btn-remove {
            background: transparent;
            border: 1px solid #dc3545;
            color: #dc3545;
            padding: 6px 15px;
            border-radius: 6px;
            font-size: 0.85rem;
            font-weight: 500;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .btn-remove:hover {
            background: #dc3545;
            color: white;
            transform: translateY(-2px);
        }

        .coupon-message {
            margin-top: 10px;
            padding: 10px 15px;
            border-radius: 8px;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 10px;
            animation: slideDown 0.3s ease;
        }

        @keyframes slideDown {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .coupon-success-message {
            background: rgba(40, 167, 69, 0.1);
            border: 1px solid rgba(40, 167, 69, 0.3);
            color: #155724;
        }

        .coupon-error-message {
            background: rgba(247, 37, 133, 0.1);
            border: 1px solid rgba(247, 37, 133, 0.3);
            color: #721c24;
        }

        /* Payment Methods */
        .payment-methods {
            margin-bottom: 30px;
        }

        .payment-method {
            border: 2px solid #eaeaea;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 15px;
            cursor: pointer;
            transition: all 0.3s ease;
            background: white;
        }

        .payment-method:hover {
            border-color: var(--primary-color);
            transform: translateY(-3px);
        }

        .payment-method.selected {
            border-color: var(--primary-color);
            background: linear-gradient(135deg, rgba(67, 97, 238, 0.05) 0%, rgba(58, 12, 163, 0.05) 100%);
        }

        .payment-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        .payment-title {
            display: flex;
            align-items: center;
            gap: 12px;
            color: var(--dark-bg);
            font-weight: 600;
        }

        .payment-icon {
            color: var(--primary-color);
            font-size: 1.2rem;
        }

        .payment-badge {
            background: var(--primary-color);
            color: white;
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .payment-description {
            color: #6c757d;
            font-size: 0.9rem;
            margin-bottom: 0;
        }

        /* Wallet Balance */
        .wallet-balance {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            margin-bottom: 25px;
        }

        .wallet-balance h6 {
            font-size: 0.95rem;
            opacity: 0.9;
            margin-bottom: 8px;
        }

        .wallet-balance .balance {
            font-size: 2rem;
            font-weight: 800;
            line-height: 1;
        }

        /* Place Order Button */
        .btn-place-order {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            border: none;
            color: white;
            padding: 18px 30px;
            font-size: 1.1rem;
            font-weight: 700;
            border-radius: 12px;
            width: 100%;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            cursor: pointer;
        }

        .btn-place-order:hover:not(:disabled) {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(67, 97, 238, 0.3);
        }

        .btn-place-order:disabled {
            background: #6c757d;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        /* Remove Coupon Button */
        .btn-remove-coupon {
            background: transparent;
            border: 1px solid #dc3545;
            color: #dc3545;
            padding: 4px 12px;
            border-radius: 6px;
            font-size: 0.85rem;
            transition: all 0.3s ease;
        }

        .btn-remove-coupon:hover {
            background: #dc3545;
            color: white;
            transform: translateY(-2px);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .checkout-page {
                padding: 20px 0;
            }
            
            .page-header {
                padding: 40px 0;
                border-radius: 0 0 20px 20px;
            }
            
            .page-header h1 {
                font-size: 2rem;
            }
            
            .checkout-container {
                padding: 25px 20px;
            }
            
            .address-grid {
                grid-template-columns: 1fr;
            }
            
            .order-summary-card {
                position: static;
                margin-top: 30px;
            }
            
            .coupon-form {
                flex-direction: column;
            }
            
            .coupon-success {
                flex-direction: column;
                gap: 10px;
                align-items: flex-start;
            }
            
            .coupon-success .d-flex {
                width: 100%;
                justify-content: space-between;
            }
        }

        /* Loading Overlay */
        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255,255,255,0.9);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            display: none;
        }

        .loading-spinner {
            width: 60px;
            height: 60px;
            border: 6px solid #f3f3f3;
            border-top: 6px solid var(--primary-color);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <!-- Include Header and Navbar -->
    <%- include('./layout/header.ejs') %>
    <%- include('./layout/navBar.ejs', { currentPage: currentPage || 'checkout' }) %>

    <div class="checkout-page">
        <!-- Page Header -->
        <div class="page-header">
            <div class="container">
                <h1><i class="fas fa-shopping-cart me-3"></i> Secure Checkout</h1>
                <p>Complete your order with safe and secure payment options</p>
            </div>
        </div>

        <div class="container">
            <!-- Breadcrumb -->
            <nav aria-label="breadcrumb" class="mb-4">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/">Home</a></li>
                    <li class="breadcrumb-item"><a href="/cart">Cart</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Checkout</li>
                </ol>
            </nav>

            <div id="checkoutForm">
                <div class="row">
                    <!-- Left Column - Billing Details -->
                    <div class="col-lg-8">
                        <div class="checkout-container">
                            <!-- Address Section -->
                            <div class="address-section">
                                <h3 class="section-title">
                                    <i class="fas fa-map-marker-alt"></i>
                                    Delivery Address
                                </h3>
                                
                                <% if (address && address.address && address.address.length > 0) { %>
                                    <div class="address-grid">
                                        <% address.address.forEach((addr, index) => { %>
                                            <div class="address-card" onclick="selectAddress('<%= addr._id %>', '<%= addr.Fname %>')" id="address-<%= addr._id %>">
                                                <div class="address-type">
                                                    <%= addr.AddressType || 'Home' %>
                                                </div>
                                                <div class="address-details">
                                                    <h6><%= addr.Fname %></h6>
                                                    <p><i class="fas fa-map-pin"></i> <%= addr.Address %>, <%= addr.Place %></p>
                                                    <p><i class="fas fa-city"></i> <%= addr.state %> - <%= addr.Pincode %></p>
                                                    <p><i class="fas fa-phone"></i> <%= addr.PhoneNumber %></p>
                                                    <% if (addr.AphoneNumber) { %>
                                                        <p><i class="fas fa-phone-alt"></i> <%= addr.AphoneNumber %></p>
                                                    <% } %>
                                                    <p><i class="fas fa-envelope"></i> <%= addr.Email %></p>
                                                </div>
                                                <input type="radio" 
                                                       class="address-radio" 
                                                       name="selectedAddress" 
                                                       id="address-radio-<%= addr._id %>"
                                                       value="<%= addr._id %>"
                                                       <%= index === 0 ? 'checked' : '' %>
                                                       style="display: none;">
                                            </div>
                                        <% }); %>
                                    </div>
                                    
                                    <div class="text-end mt-3">
                                        <a href="/userProfile/addresses" class="btn-add-address">
                                            <i class="fas fa-plus"></i> Add New Address
                                        </a>
                                    </div>
                                <% } else { %>
                                    <div class="no-address">
                                        <i class="fas fa-map-marked-alt"></i>
                                        <h4>No Address Found</h4>
                                        <p>You need to add a delivery address before proceeding with your order.</p>
                                        <a href="/userProfile/addresses" class="btn-add-address">
                                            <i class="fas fa-plus"></i> Add Delivery Address
                                        </a>
                                    </div>
                                <% } %>
                            </div>

                            <!-- Payment Methods Section -->
                            <div class="payment-methods">
                                <!-- Wallet Payment -->
                                <% if (walletBalance >= allTotal) { %>
                                    <div class="payment-method" onclick="selectPaymentMethod('Wallet')" id="payment-wallet">
                                        <div class="payment-header">
                                            <div class="payment-title">
                                                <i class="fas fa-wallet payment-icon"></i>
                                                <span>Pay with Wallet</span>
                                            </div>
                                            <span class="payment-badge">₹<%= walletBalance %> Available</span>
                                        </div>
                                        <p class="payment-description">
                                            Use your wallet balance to pay for this order instantly.
                                        </p>
                                    </div>
                                <% } %>
                                
                                <!-- Cash on Delivery -->
                                <div class="payment-method" onclick="selectPaymentMethod('Cash on Delivery')" id="payment-cash-on-delivery">
                                    <div class="payment-header">
                                        <div class="payment-title">
                                            <i class="fas fa-money-bill-wave payment-icon"></i>
                                            <span>Cash on Delivery</span>
                                        </div>
                                    </div>
                                    <p class="payment-description">
                                        Pay when your order is delivered. No advance payment required.
                                    </p>
                                </div>
                                
                                <!-- Online Payment -->
                                <div class="payment-method" onclick="selectPaymentMethod('Online Payment')" id="payment-online-payment">
                                    <div class="payment-header">
                                        <div class="payment-title">
                                            <i class="fas fa-globe payment-icon"></i>
                                            <span>Online Payment</span>
                                        </div>
                                    </div>
                                    <p class="payment-description">
                                        Secure payment via RazorPay. Cards, UPI, Net Banking accepted.
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Right Column - Order Summary -->
                    <div class="col-lg-4">
                        <div class="order-summary-card">
                            <div class="summary-header">
                                <h4><i class="fas fa-receipt me-2"></i> Order Summary</h4>
                                <p>Review your items and total</p>
                            </div>

                            <!-- Products List -->
                            <div class="products-list">
                                <% if (products && products.length > 0) { %>
                                    <% products.forEach((product, index) => { %>
                                        <div class="product-item">
                                            <div class="product-name">
                                                <%= product.name || 'Product' %>
                                                <small class="text-muted d-block">Qty: <%= product.quantity || product.purchasedCount || 1 %></small>
                                            </div>
                                            <div class="product-price">
                                                ₹<%= individualTotalArray[index] || 0 %>
                                            </div>
                                        </div>
                                    <% }); %>
                                <% } else { %>
                                    <p class="text-center text-muted">No products in cart</p>
                                <% } %>
                            </div>

                            <!-- Price Breakdown -->
                            <div class="price-breakdown">
                                <% 
                                    // Calculate subtotal for display
                                    let subtotal = 0;
                                    if (individualTotalArray && individualTotalArray.length > 0) {
                                        subtotal = individualTotalArray.reduce((a, b) => a + b, 0);
                                    }
                                %>
                                <div class="price-row">
                                    <span class="price-label">Subtotal</span>
                                    <span class="price-value">₹<%= subtotal %></span>
                                </div>
                                <div class="price-row">
                                    <span class="price-label">Shipping</span>
                                    <span class="price-value">₹<%= shippingPrice || 0 %></span>
                                </div>
                                <div id="discountSection" <% if (discountAmount <= 0) { %>style="display: none;"<% } %>>
                                    <div class="price-row">
                                        <span class="price-label">Discount (<span id="discountPercentage"><%= discountPercentage %></span>% off)</span>
                                        <span class="price-value text-success" id="discountAmount">-₹<%= discountAmount || 0 %></span>
                                    </div>
                                </div>
                                <hr>
                                <div class="price-row">
                                    <span class="price-label price-total">Total Amount</span>
                                    <span class="price-value price-total" id="totalAmount">₹<%= allTotal || 0 %></span>
                                </div>
                            </div>

                            <!-- Coupon Section -->
                            <div class="coupon-section">
                                <div id="couponContainer">
                                    <% if (discountSuccess && couponCode) { %>
                                        <div class="coupon-applied">
                                            <div class="coupon-success">
                                                <div class="d-flex align-items-center">
                                                    <span class="coupon-code">
                                                        <i class="fas fa-ticket-alt"></i>
                                                        <%= couponCode %>
                                                    </span>
                                                    <span class="coupon-discount ms-3">
                                                        -₹<%= discountAmount %> (<%= discountPercentage %>% off)
                                                    </span>
                                                </div>
                                                <button type="button" class="btn-remove" onclick="removeCoupon()">
                                                    Remove
                                                </button>
                                            </div>
                                        </div>
                                    <% } else { %>
                                        <div id="couponForm">
                                            <div class="coupon-form">
                                                <input type="text" 
                                                       class="coupon-input" 
                                                       id="couponCodeInput" 
                                                       name="couponCode" 
                                                       placeholder="Enter coupon code"
                                                       value="<%= couponCode || '' %>">
                                                <button type="button" class="btn-apply" id="applyCouponBtn">
                                                    Apply
                                                </button>
                                            </div>
                                            <% if (discountError && !discountSuccess) { %>
                                                <div class="coupon-message coupon-error-message" id="couponError">
                                                    <i class="fas fa-exclamation-circle"></i>
                                                    <%= discountError %>
                                                </div>
                                            <% } %>
                                        </div>
                                    <% } %>
                                </div>
                                <div id="couponMessage"></div>
                            </div>

                            <!-- Place Order Button -->
                            <button type="button" class="btn-place-order" id="placeOrderBtn" <%= (!address || !address.address || address.address.length === 0) ? 'disabled' : '' %>>
                                <i class="fas fa-lock"></i>
                                <span id="btnText">Place Secure Order</span>
                            </button>

                            <!-- Security Note -->
                            <div class="text-center mt-3">
                                <small class="text-muted">
                                    <i class="fas fa-shield-alt me-1"></i>
                                    100% Secure Payment • SSL Encrypted
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Loading Overlay -->
    <div class="loading-overlay" id="loadingOverlay">
        <div class="loading-spinner"></div>
    </div>

    <!-- Include Mobile Navbar and Footer -->
    <%- include('./layout/navBarMobile.ejs') %>
    <%- include('./layout/footer.ejs') %>
    
    <!-- JavaScript -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
    
    <script>
        $(document).ready(function() {
            // Initialize variables
            let selectedAddressId = '';
            let selectedAddressName = '';
            let selectedPaymentMethod = '';
            let isCouponApplying = false;
            
            // Address selection
            window.selectAddress = function(addressId, fname) {
                console.log('Selecting address:', addressId, fname);
                
                // Remove selected class from all addresses
                $('.address-card').removeClass('selected');
                // Add selected class to clicked address
                $('#address-' + addressId).addClass('selected');
                // Check the radio button
                $('#address-radio-' + addressId).prop('checked', true);
                
                // Set hidden fields
                selectedAddressId = addressId;
                selectedAddressName = fname;
                
                // Enable place order button if payment method is also selected
                checkFormCompletion();
                
                console.log('Address selected:', addressId, fname);
            }
            
            // Payment method selection
            window.selectPaymentMethod = function(paymentMethod) {
                console.log('Selecting payment method:', paymentMethod);
                
                // Remove selected class from all payment methods
                $('.payment-method').removeClass('selected');
                
                // Create selector ID based on payment method
                const paymentMethodId = paymentMethod.toLowerCase().replace(/ /g, '-');
                const selectorId = '#payment-' + paymentMethodId;
                
                console.log('Looking for selector:', selectorId);
                
                // Add selected class to clicked payment method
                if ($(selectorId).length) {
                    $(selectorId).addClass('selected');
                    console.log('Payment method element found and selected');
                } else {
                    console.error('Payment method element not found:', selectorId);
                    // Try alternative selectors
                    const altSelector = '.payment-method:contains("' + paymentMethod + '")';
                    $(altSelector).addClass('selected');
                }
                
                // Set hidden field
                selectedPaymentMethod = paymentMethod;
                
                // Enable place order button if address is also selected
                checkFormCompletion();
                
                console.log('Payment method selected:', paymentMethod);
            }
            
            // Remove coupon function
            window.removeCoupon = function() {
                if (isCouponApplying) return;
                
                // Show loading
                $('#loadingOverlay').show();
                
                $.ajax({
                    type: 'GET',
                    url: '/order/remove-coupon',
                    success: function(response) {
                        if (response.success) {
                            // Reload page to update all totals
                            window.location.reload();
                        } else {
                            $('#loadingOverlay').hide();
                            showCouponError(response.error || 'Error removing coupon');
                        }
                    },
                    error: function() {
                        $('#loadingOverlay').hide();
                        // Still reload page
                        window.location.reload();
                    }
                });
            }
            
            // Check if form is complete
            function checkFormCompletion() {
                console.log('Checking form completion:', {
                    address: selectedAddressId,
                    payment: selectedPaymentMethod
                });
                
                if (selectedAddressId && selectedPaymentMethod) {
                    $('#placeOrderBtn').prop('disabled', false);
                    console.log('Form is complete, enabling button');
                } else {
                    $('#placeOrderBtn').prop('disabled', true);
                    console.log('Form incomplete, disabling button');
                }
            }
            
            // Initialize form on page load
            function initializeForm() {
                // Set first address as selected by default
                if ($('.address-card').length > 0) {
                    const firstAddress = $('.address-card').first();
                    const firstAddressId = firstAddress.attr('id').replace('address-', '');
                    const firstAddressName = firstAddress.find('h6').text().trim();
                    selectAddress(firstAddressId, firstAddressName);
                }
                
                // Set default payment method
                const walletBalance = <%= walletBalance || 0 %>;
                const allTotal = <%= allTotal || 0 %>;
                
                if (walletBalance >= allTotal) {
                    selectPaymentMethod('Wallet');
                } else {
                    selectPaymentMethod('Online Payment');
                }
                
                // Set initial button state
                checkFormCompletion();
            }
            
            // Initialize the form
            initializeForm();
            
            // Handle address card clicks
            $('.address-card').on('click', function() {
                const addressId = $(this).attr('id').replace('address-', '');
                const addressName = $(this).find('h6').text().trim();
                selectAddress(addressId, addressName);
            });
            
            // Handle payment method clicks
            $('.payment-method').on('click', function() {
                const methodText = $(this).find('.payment-title span').text().trim();
                selectPaymentMethod(methodText);
            });
            
            // Function to show coupon error
            function showCouponError(message) {
                $('#couponMessage').html(`
                    <div class="coupon-message coupon-error-message">
                        <i class="fas fa-exclamation-circle"></i>
                        ${message}
                    </div>
                `).fadeIn();
                
                // Auto-hide after 5 seconds
                setTimeout(function() {
                    $('#couponMessage').fadeOut();
                }, 5000);
            }
            
            // Function to show coupon success
            function showCouponSuccess(message) {
                $('#couponMessage').html(`
                    <div class="coupon-message coupon-success-message">
                        <i class="fas fa-check-circle"></i>
                        ${message}
                    </div>
                `).fadeIn();
                
                // Auto-hide after 5 seconds
                setTimeout(function() {
                    $('#couponMessage').fadeOut();
                }, 5000);
            }
            
            // Function to update order totals
            function updateOrderTotals(discountAmount, discountPercentage, couponCode) {
                // Get current subtotal
                const subtotal = <%= subtotal %>;
                const shippingPrice = <%= shippingPrice || 0 %>;
                
                // Calculate new totals
                const total = subtotal + shippingPrice - discountAmount;
                
                // Update UI
                $('#discountPercentage').text(discountPercentage);
                $('#discountAmount').text('-₹' + discountAmount);
                $('#totalAmount').text('₹' + total);
                
                // Show discount section
                $('#discountSection').show();
                
                // Update coupon section
                $('#couponContainer').html(`
                    <div class="coupon-applied">
                        <div class="coupon-success">
                            <div class="d-flex align-items-center">
                                <span class="coupon-code">
                                    <i class="fas fa-ticket-alt"></i>
                                    ${couponCode}
                                </span>
                                <span class="coupon-discount ms-3">
                                    -₹${discountAmount} (${discountPercentage}% off)
                                </span>
                            </div>
                            <button type="button" class="btn-remove" onclick="removeCoupon()">
                                Remove
                            </button>
                        </div>
                    </div>
                `);
            }
            
            // Handle coupon application
            $('#applyCouponBtn').on('click', function() {
                if (isCouponApplying) return;
                
                const couponCode = $('#couponCodeInput').val().trim();
                
                if (!couponCode) {
                    showCouponError("Please enter a coupon code");
                    return;
                }
                
                // Disable apply button and show loading
                isCouponApplying = true;
                $('#applyCouponBtn').prop('disabled', true).html('Applying...');
                $('#couponError').remove();
                $('#couponMessage').empty();
                
                $.ajax({
                    type: 'POST',
                    url: '/order/apply-coupon',
                    data: { couponCode: couponCode },
                    success: function(response) {
                        isCouponApplying = false;
                        $('#applyCouponBtn').prop('disabled', false).html('Apply');
                        
                        if (response.success) {
                            // Show success message
                            showCouponSuccess(response.message || "Coupon applied successfully!");
                            
                            // Update order totals without page reload
                            updateOrderTotals(
                                response.discountAmount,
                                response.discountPercentage,
                                response.couponCode
                            );
                        } else {
                            showCouponError(response.error || "Error applying coupon");
                        }
                    },
                    error: function(xhr, status, error) {
                        isCouponApplying = false;
                        $('#applyCouponBtn').prop('disabled', false).html('Apply');
                        
                        console.error('Coupon AJAX Error:', error);
                        let errorMessage = 'Error applying coupon. Please try again.';
                        
                        if (xhr.responseJSON && xhr.responseJSON.error) {
                            errorMessage = xhr.responseJSON.error;
                        }
                        
                        showCouponError(errorMessage);
                    }
                });
            });
            
            // Handle Enter key in coupon input
            $('#couponCodeInput').on('keypress', function(e) {
                if (e.which === 13) {
                    e.preventDefault();
                    $('#applyCouponBtn').click();
                }
            });
            
            // Main place order button click
            $('#placeOrderBtn').on('click', function(e) {
                e.preventDefault();
                
                // Prevent if coupon is being applied
                if (isCouponApplying) {
                    Swal.fire({
                        icon: 'warning',
                        title: 'Please wait',
                        text: 'Coupon is being applied. Please wait.',
                        confirmButtonColor: '#4361ee'
                    });
                    return false;
                }
                
                console.log('Place order button clicked');
                console.log('Selected address:', selectedAddressId);
                console.log('Selected payment:', selectedPaymentMethod);
                
                // Validate address selection
                if (!selectedAddressId) {
                    Swal.fire({
                        icon: 'error',
                        title: 'Address Required',
                        text: 'Please select a delivery address',
                        confirmButtonColor: '#4361ee'
                    });
                    return false;
                }
                
                // Validate payment method selection
                if (!selectedPaymentMethod) {
                    Swal.fire({
                        icon: 'error',
                        title: 'Payment Method Required',
                        text: 'Please select a payment method',
                        confirmButtonColor: '#4361ee'
                    });
                    return false;
                }
                
                // Show loading
                $('#loadingOverlay').show();
                $('#btnText').text('Processing...');
                $('#placeOrderBtn').prop('disabled', true);
                
                const orderData = {
                    selectedAddressId: selectedAddressId,
                    selectedAddressName: selectedAddressName,
                    selectedPaymentMethod: selectedPaymentMethod
                };
                
                console.log('Sending order data to /order/order:', orderData);
                
                // Make AJAX request
                $.ajax({
                    type: 'POST',
                    url: '/order/order',
                    data: orderData,
                    success: function(response) {
                        console.log('Response from /order/order:', response);
                        
                        if (response.codSucess || response.walletSucess) {
                            // Clear session data on success
                            $.ajax({
                                url: '/order/clearSessionDiscount',
                                method: 'GET',
                                complete: function() {
                                    window.location.href = '/orderSucess';
                                }
                            });
                        } else if (response.data) {
                            // Razorpay payment
                            processRazorpayPayment(response.data);
                        } else if (response.error) {
                            $('#loadingOverlay').hide();
                            $('#btnText').text('Place Secure Order');
                            $('#placeOrderBtn').prop('disabled', false);
                            
                            Swal.fire({
                                icon: 'error',
                                title: 'Order Failed',
                                text: response.error || 'Something went wrong. Please try again.',
                                confirmButtonColor: '#4361ee'
                            });
                        } else {
                            $('#loadingOverlay').hide();
                            $('#btnText').text('Place Secure Order');
                            $('#placeOrderBtn').prop('disabled', false);
                            
                            Swal.fire({
                                icon: 'error',
                                title: 'Unknown Response',
                                text: 'Unexpected response from server',
                                confirmButtonColor: '#4361ee'
                            });
                        }
                    },
                    error: function(xhr, status, error) {
                        $('#loadingOverlay').hide();
                        $('#btnText').text('Place Secure Order');
                        $('#placeOrderBtn').prop('disabled', false);
                        
                        console.error('AJAX Error:', error);
                        console.error('Status:', status);
                        console.error('Response:', xhr.responseText);
                        
                        let errorMessage = 'An error occurred while processing your order. Please try again.';
                        
                        if (xhr.responseJSON && xhr.responseJSON.error) {
                            errorMessage = xhr.responseJSON.error;
                        }
                        
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: errorMessage,
                            confirmButtonColor: '#4361ee'
                        });
                    }
                });
            });
            
            // Razorpay payment processing
            function processRazorpayPayment(order) {
                console.log('Processing Razorpay payment:', order);
                
                var options = {
                    "key": "rzp_test_SoEmBjOco04Lhe",
                    "amount": order.amount * 100,
                    "currency": "INR",
                    "name": "Lexxive",
                    "description": "Order Payment",
                    "image": "/assets/images/logo.png",
                    "order_id": order.id,
                    "handler": function(response) {
                        console.log('Razorpay payment successful:', response);
                        verifyPayment(response, order);
                    },
                    "prefill": {
                        "name": "<%= user.Fname %> <%= user.Lname %>",
                        "email": "<%= user.Email %>",
                        "contact": "<%= user.PhoneNumber %>"
                    },
                    "theme": {
                        "color": "#4361ee"
                    },
                    "modal": {
                        "ondismiss": function() {
                            $('#loadingOverlay').hide();
                            $('#btnText').text('Place Secure Order');
                            $('#placeOrderBtn').prop('disabled', false);
                        }
                    }
                };
                
                var rzp1 = new Razorpay(options);
                rzp1.open();
            }
            
            // Verify payment
            function verifyPayment(payment, order) {
                $('#loadingOverlay').show();
                
                $.ajax({
                    method: 'POST',
                    url: '/order/verifyPayment',
                    data: {
                        payment: payment,
                        order: order
                    },
                    success: function(response) {
                        $('#loadingOverlay').hide();
                        if (response.status) {
                            // Clear session data on success
                            $.ajax({
                                url: '/order/clearSessionDiscount',
                                method: 'GET',
                                complete: function() {
                                    window.location.href = '/orderSucess';
                                }
                            });
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'Payment Failed',
                                text: 'Payment verification failed. Please try again.',
                                confirmButtonColor: '#4361ee'
                            });
                        }
                    },
                    error: function() {
                        $('#loadingOverlay').hide();
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: 'Error verifying payment. Please contact support.',
                            confirmButtonColor: '#4361ee'
                        });
                    }
                });
            }
            
            // Debug: Log initial state
            console.log('Initial state - Wallet Balance:', <%= walletBalance || 0 %>, 'Total:', <%= allTotal || 0 %>);
        });
    </script>
</body>
</html>