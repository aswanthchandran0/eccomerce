<%- include('./layout/header.ejs')-%>
<% currentPage = 'Category' %>
    <div class="container-scroller">
      <!-- partial:partials/_sidebar.html -->
    <%- include('./layout/sideBar.ejs') -%>
      <!-- partial -->
      <div class="container-fluid page-body-wrapper">
        <!-- partial:partials/_navbar.html -->
     
        <!-- partial -->
        <div class="main-panel">
          <div class="content-wrapper">
           
            <div class="row">
             
              <div class="col-md-5 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    <div class="d-flex flex-row justify-content-between">
                      <h4 class="card-title mb-1">Category</h4>
                      <p class="text-muted mb-1"></p>
                    </div>
                    <div class="row">
                      <div class="col-12">
                        <div class="preview-list">
                          <!---->
                         
                          
                            
                               
                               
                                <div class="table-responsive">
                                    <table class="table" style="width: 100%;  border-radius: 10px;" >
                                        <thead>
                                            <tr>
                                                <th> # </th>
                                                <th> Category </th>
                                                <th></th>
                                             
                                                <th>
                                                  <div class="badge badge-outline-success">  <a href="/admin/addCatagory"
                                                    style="color:rgba(107, 235, 164, 1); background: none; border: none; ">Add</a></div>
                                                </th>
                                            </tr>
                                        </thead>
                                        <tbody id="#list">
                                            <% Catagories.forEach((category, index) => { %>
                                            <tr class="user-row">
                                                <td id="displayedIndex"><%= index + 1 %></td>
                                                <td id="name"><%= category.name %> </td>
                                                <td>
                                            
                                                <td>
                                                    <form  id="deleteForm_<%= category._id %>" action="/admin/catagory/delete/<%= category._id %>" method="POST" style="display: inline;">
                                                      <div class="badge badge-outline-danger">
                                                        <button type="button"
                                                            style="color: rgba(245, 37, 37, 1); background: none; border: none;" onclick=" return confirmDelete('<%= category._id %>','<%= category.name %>')">Delete</button></div>

                                                    </form>
                                                </td>
                                            </tr>
                                          
                                            <% }); %>
                                        </tbody>
                                    </table>
                                </div>
                          
                       
            
                         <!---->

                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
           
           


          </div>
          <!-- content-wrapper ends -->
          <!-- partial:partials/_footer.html -->
         
          <!-- partial -->
        </div>
        <!-- main-panel ends -->
      </div>
      <!-- page-body-wrapper ends -->
    </div>




    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    <!-- Include your custom styles and script -->
    <style>
        .custom-swal-popup {
            background-color: #38455e !important;
            color: #fff;
        }
    </style>

<script>
  function confirmDelete(userId, userName) {
      Swal.fire({
          title: 'Delete Confirmation',
          text: `Are you sure you want to delete ${userName}?`,
          showCancelButton: true,
          confirmButtonColor: '#f44336',
          cancelButtonColor: '#2196f3',
          confirmButtonText: 'Yes, delete it!',
          customContainerClass: 'custom-swal',
          customClass: {
              popup: 'custom-swal-popup',
              content: 'custom-swal-content',
              confirmButton: 'custom-swal-confirm-button'
          }
      }).then((result) => {
          if (result.isConfirmed) {
              // Perform the form submission using JavaScript
              const deleteForm = document.getElementById(`deleteForm_${userId}`);
              if (deleteForm) {
                  deleteForm.submit();
              } else {
                  console.error(`Form with ID deleteForm_${userId} not found.`);
              }
          }
      });
  }
</script>

    <script>
        document.addEventListener("DOMContentLoaded", () => {

            // Select the search input field
            const productSearchInput = document.getElementById('searchInput');

            // Select all product rows
            const productRows = document.querySelectorAll('.user-row');

            productSearchInput.addEventListener('input', () => {
                const searchTerm = productSearchInput.value.toLowerCase();

                // Loop through all product rows and hide those that don't match the search term
                productRows.forEach((row, index) => {
                    const productName = row.querySelector('#name').textContent.toLowerCase();
                    const productCategory = row.querySelector('#email').textContent.toLowerCase();

                    if (
                        productName.includes(searchTerm) ||
                        productCategory.includes(searchTerm)
                    ) {
                        row.style.display = 'table-row';
                        const displayedIndex = index + 1;
                        row.querySelector('#displayedIndex').textContent = displayedIndex;
                    } else {
                        row.style.display = 'none';
                    }
                });
            });

        })
    </script>














    <%- include('./layout/footer.ejs')-%>